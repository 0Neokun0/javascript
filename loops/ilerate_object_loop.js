// OBJECTS
const student = {
    name: "Nishant",
    class: "処理ネットワーク専攻",
    porject: "Upcolor"
}

// Using for ... in
for ( let i in student ) {

    // Display The Properties
    console.log(`${i} => ${student[i]}`);
}

// END